services:
  sfstack-postgresql:
    container_name: sfstack-postgresql
    image: postgres:16.4
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: postgres
    volumes:
      - sfstack-postgresql-data:/var/lib/postgresql/data

volumes:
  sfstack-postgresql-data:
